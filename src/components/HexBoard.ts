import {GraphicalComponent} from '@/utils/component'
import {HexTetris} from '@/game/scenes/HexTetris'

const boardSize = {
    w: 11, h: 11
}

const HexBoard: GraphicalComponent<HexTetris> = ({scene}) => {

}